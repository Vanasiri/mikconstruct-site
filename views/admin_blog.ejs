<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <title>Admin – จัดการบทความ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

    <%- include('partials/admin_nav') %>

        <div class="container py-4">

            <h2 class="mb-4">➕ เพิ่มบทความใหม่</h2>
            <form action="/admin/blog/add" method="POST" enctype="multipart/form-data"
                class="card card-body shadow-sm mb-5">
                <div class="mb-3">
                    <label class="form-label">หัวข้อ</label>
                    <input type="text" name="title" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">สรุปย่อ (Excerpt)</label>
                    <textarea name="excerpt" class="form-control" rows="2" required></textarea>
                </div>
                <div class="mb-3">
                    <label class="form-label">เนื้อหาเต็ม</label>
                    <textarea name="content" class="form-control" rows="5" required></textarea>
                </div>
                <div class="mb-3">
                    <label class="form-label">รูปภาพ (สูงสุด 5 รูป)</label>
                    <input type="file" name="images" class="form-control" multiple accept="image/*">
                </div>
                <button class="btn btn-success">เพิ่มบทความ</button>
            </form>

            <h2 class="mb-3">🗂️ บทความทั้งหมด</h2>
            <% posts.forEach(post=> { %>
                <div class="card mb-3 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= post.title %>
                        </h5>
                        <p>
                            <%= post.excerpt %>
                        </p>

                        <form action="/admin/blog/delete/<%= post.id %>" method="POST"
                            onsubmit="return confirm('ยืนยันลบบทความนี้?');" class="d-inline-block me-2">
                            <button class="btn btn-danger btn-sm">ลบ</button>
                        </form>

                        <!-- ฟอร์มแก้ไข -->
                        <form action="/admin/blog/edit/<%= post.id %>" method="POST" enctype="multipart/form-data"
                            class="mt-3">
                            <div class="mb-2">
                                <input type="text" name="title" value="<%= post.title %>" class="form-control" required>
                            </div>
                            <div class="mb-2">
                                <textarea name="excerpt" class="form-control" rows="2"
                                    required><%= post.excerpt %></textarea>
                            </div>
                            <div class="mb-2">
                                <textarea name="content" class="form-control" rows="4"
                                    required><%= post.content %></textarea>
                            </div>

                            <% if (post.images && post.images.length) { %>
                                <% post.images.forEach(img=> { %>
                                    <div class="mb-2">
                                        <img src="<%= img.image_path %>" class="img-thumbnail mb-1"
                                            style="max-height: 120px;">
                                        <label class="form-check-label ms-2">
                                            <input type="checkbox" name="delete[<%= img.id %>]"
                                                class="form-check-input"> ลบรูปนี้
                                        </label>
                                    </div>
                                    <% }) %>
                                        <% } %>

                                            <div class="mb-2">
                                                <input type="file" name="images" class="form-control" multiple
                                                    accept="image/*">
                                                <small class="text-muted">เพิ่มรูปใหม่ (สูงสุด 5 รูป)</small>
                                            </div>

                                            <button class="btn btn-primary btn-sm">บันทึกการแก้ไข</button>
                        </form>

                    </div>
                </div>
                <% }) %>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>