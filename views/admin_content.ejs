<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Admin – Content</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <style>
    body { min-height: 100vh; }
    .sidebar {
      min-height: calc(100vh - 56px); /* ลบความสูง navbar */
      border-right: 1px solid #dee2e6;
    }
    .sidebar .nav-link {
      margin-bottom: .5rem;
    }
  </style>
</head>
<body class="bg-light">

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/admin/content">Admin Panel</a>
      <div class="ms-auto">
        <a class="btn btn-outline-light" href="/logout">ออกจากระบบ</a>
      </div>
    </div>
  </nav>
  
  <div class="container-fluid">
    <div class="row">

      <!-- Sidebar -->
      <aside class="col-md-2 bg-white sidebar py-4">
        <nav class="nav flex-column">
          <a class="nav-link active" href="/admin/content">🔧 แก้ไขข้อความ</a>
          <a class="nav-link" href="/admin/projects">📦 จัดการโปรเจกต์</a>
          <a class="nav-link" href="/admin/services">🛠️ จัดการ Services</a>
		<a class="nav-link" href="/admin/blog">✍️ จัดการ Blog</a>
          <a 
            class="nav-link" 
            data-bs-toggle="collapse" 
            href="#heroMenu" 
            role="button"
            aria-expanded="false"
          >
            🎨 Hero Images
          </a>
          <div class="collapse ps-3" id="heroMenu">
            <% heroSections.forEach(h => { %>
              <a class="nav-link small" href="/admin/images/<%= h.route %>">
                • เปลี่ยน <%= h.title %>
              </a>
            <% }) %>
          </div>
        </nav>
      </aside>

      <!-- Main Content -->
      <main class="col-md-10 py-4">
        <h2 class="mb-4">🔧 แก้ไขข้อความ</h2>
        <div class="accordion" id="contentAccordion">
          <% contents.forEach(item => { %>
          <div class="accordion-item mb-2 shadow-sm">
            <h2 class="accordion-header" id="heading<%= item.id %>">
              <button
                class="accordion-button collapsed"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#collapse<%= item.id %>"
                aria-expanded="false"
                aria-controls="collapse<%= item.id %>"
              >
                <%= item.page %> / <%= item.section %>
              </button>
            </h2>
            <div
              id="collapse<%= item.id %>"
              class="accordion-collapse collapse"
              data-bs-parent="#contentAccordion"
            >
              <div class="accordion-body">
                <form method="POST" action="/admin/update" class="row g-2 align-items-end">
                  <input type="hidden" name="id" value="<%= item.id %>">
                  <div class="col-12">
                    <textarea
                      class="form-control"
                      name="text"
                      rows="3"
                    ><%= item.text %></textarea>
                  </div>
                  <div class="col-auto">
                    <button class="btn btn-success">บันทึก</button>
                  </div>
                </form>
              </div>
            </div>
          </div>
          <% }) %>
        </div>
      </main>

    </div>
  </div>

  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
  ></script>
</body>
</html>
