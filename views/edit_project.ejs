<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>แก้ไขโปรเจกต์</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
</head>
<body class="bg-light">

  <%- include('partials/admin_nav') %>

  <div class="container py-4">
    <h2 class="mb-4">✏️ แก้ไขโปรเจกต์: <%= project.name %></h2>

    <form
      action="/admin/projects/edit/<%= project.id %>"
      method="POST"
      enctype="multipart/form-data"
      class="card card-body mb-4 shadow-sm"
    >
      <!-- ชื่อ + หมวดหมู่ -->
      <div class="row g-3">
        <div class="col-md-6">
          <label class="form-label">ชื่อโปรเจกต์</label>
          <input
            type="text"
            name="name"
            class="form-control"
            value="<%= project.name %>"
            required
          />
        </div>
        <div class="col-md-6">
          <label class="form-label">หมวดหมู่</label>
          <input
            type="text"
            name="category"
            class="form-control"
            value="<%= project.category %>"
            required
          />
        </div>
      </div>

      <!-- รูปเดิม: ลำดับ + ลบ -->
      <div class="mt-4">
        <h5>รูปภาพเดิม</h5>
        <% images.forEach(img => { %>
        <div class="d-flex align-items-center mb-2">
          <img
            src="<%= img.image_path %>"
            alt=""
            class="me-3"
            style="width:100px; height:60px; object-fit:cover;"
          />
          <div class="me-3">
            <label class="form-label">ลำดับ</label>
            <input
              type="number"
              name="seq[<%= img.id %>]"
              class="form-control"
              style="width: 80px;"
              value="<%= img.seq %>"
            />
          </div>
          <div>
            <label class="form-check-label">
              <input
                type="checkbox"
                name="delete[<%= img.id %>]"
                class="form-check-input"
              /> ลบ
            </label>
          </div>
        </div>
        <% }) %>
      </div>

      <!-- เพิ่มรูปใหม่ -->
      <div class="mt-4">
        <h5>อัปโหลดรูปใหม่ (ไม่เกิน 50)</h5>
        <input
          type="file"
          name="new_images"
          class="form-control"
          multiple
          onchange="if(this.files.length > 50){ alert('เลือกได้ไม่เกิน 50 รูป'); this.value=''; }"
        />
        <small class="text-muted">ไม่เกิน 50 รูป</small>
      </div>

      <button class="btn btn-success mt-4">บันทึกการแก้ไข</button>
    </form>
  </div>

  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
  ></script>
</body>
</html>
