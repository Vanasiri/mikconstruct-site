<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>Admin – เปลี่ยนรูป <%= title %></title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
</head>
<body class="bg-light">

  <%- include('partials/admin_nav') %>

  <div class="container py-4">

    <h2 class="mb-4">🎨 เปลี่ยนรูป <%= title %></h2>

    <% if (path) { %>
      <div class="mb-3">
        <label class="form-label">รูปปัจจุบัน:</label><br/>
        <img src="<%= path %>" class="img-fluid rounded" style="max-height:200px;" alt="<%= title %>">
      </div>
    <% } %>

    <form
      class="d-flex align-items-center"
      action="/admin/image/<%= route %>"
      method="POST"
      enctype="multipart/form-data"
    >
      <input
        type="file"
        name="image"
        accept="image/*"
        required
        class="form-control w-auto me-3"
      />
      <button class="btn btn-primary">อัปโหลด</button>
    </form>

  </div>

  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
  ></script>
</body>
</html>
